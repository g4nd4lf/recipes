{% extends 'base.html.twig' %}

{% block body %}
    <h1>Taggroup</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tagGroup.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tagGroup.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_tag-group_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_tag-group_edit', { 'id': tagGroup.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
